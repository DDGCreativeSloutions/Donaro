{
  "name": "donations-backend",
  "version": "1.0.0",
  "description": "Backend for Donaro App",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "build": "prisma generate --schema=./prisma/schema.prod.prisma && prisma migrate deploy --schema=./prisma/schema.prod.prisma",
    "prebuild": "prisma generate --schema=./prisma/schema.prod.prisma",
    "postinstall": "echo 'Skipping postinstall script to prevent hanging'",
    "migrate": "prisma migrate dev",
    "migrate:prod": "node migrate-prod.js",
    "studio": "prisma studio",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "test:integration": "jest --testPathPattern=integration",
    "test:unit": "jest --testPathPattern=unit",
    "test:load": "artillery run __tests__/load/api-load-test.yml",
    "test:security": "node __tests__/security/api-security-test.js"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "@prisma/extension-accelerate": "^1.2.1",
    "bcrypt": "^5.1.1",
    "bcryptjs": "^3.0.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-rate-limit": "^8.1.0",
    "jsonwebtoken": "^9.0.1",
    "multer": "^1.4.5-lts.1",
    "nodemailer": "^7.0.9",
    "serverless-http": "^3.2.0",
    "sharp": "^0.32.4",
    "socket.io": "^4.7.2",
    "twilio": "^5.10.3"
  },
  "devDependencies": {
    "@types/jest": "^29.5.8",
    "artillery": "^2.0.3",
    "jest": "^29.7.0",
    "nodemon": "^3.0.1",
    "prisma": "^5.22.0",
    "supertest": "^6.3.3"
  },
  "jest": {
    "testEnvironment": "node",
    "setupFilesAfterEnv": [
      "<rootDir>/__tests__/setup.js"
    ],
    "collectCoverageFrom": [
      "src/**/*.js",
      "!src/server.js",
      "!**/node_modules/**"
    ],
    "coverageThreshold": {
      "global": {
        "branches": 75,
        "functions": 75,
        "lines": 75,
        "statements": 75
      }
    }
  }
}